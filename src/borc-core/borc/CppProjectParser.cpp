
#include "CppProjectParser.hpp"
#include "CppProject.hpp"

namespace borc {
    std::unique_ptr<Project> CppProjectParser::parser(const std::string &file) {
        std::unique_ptr<CppProject> project;
        
        return project;
    }
}
